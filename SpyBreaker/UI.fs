namespace SpyBreaker

open SpyBreaker.UICommon
open SpyBreaker.UIScene

module UI =
    let isTrue = UICommon.isTrue
    let hintButtonRect = UICommon.hintButtonRect
    let drawScene = UIScene.drawScene
